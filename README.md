# Ai-Fraud-Detection-System
• Engineered an ML pipeline to detect fraudulent transactions using pattern recognition, anomaly detection, and ensemble methods (Random Forest, XGBoost). • Handled class imbalance via SMOTE and optimised precision-recall across the full ML lifecycle: preprocessing, feature engineering, and hyperparameter tuning.
